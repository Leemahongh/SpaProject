{% extends 'base.html.twig' %}

{% block body %}

    <div class="cover-section">
        <div class="container">
            <div class="d-flex flex-row justify-content-center login">
                <form action="{{ path('newuser') }}" method="post">
                    <div class="form-group">
                        <label class="green" for="username">Nom</label>
                        <input type="text" id="nom" name="_nom" class="form-control" value="{{ NOM }}">
                    </div>
                    <div class="form-group">
                        <label for="password">Pr&eacute;nom</label>
                        <input type="text" id="prenom" name="_prenom" class="form-control" value="{{ PRENOM }}">
                    </div>

                    <button type="submit" class="btn btn-success">S'enregistrer !</button>
                </form>
            </div>
        </div>
    </div>

{% endblock %}

{% block title %}
    Enregistrement
{% endblock %}

